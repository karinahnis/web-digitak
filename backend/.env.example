# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=web_digitak

# JWT
JWT_SECRET=your_strong_random_secret_here_change_this_in_production
JWT_EXPIRES_IN=14400

# Base URL (used for building full image URLs)
BASE_URL=http://localhost:3000

# Seed only — used by docs/seed.js
# Set this before running: node docs/seed.js
ADMIN_SEED_PASSWORD=
